<form method="post" action="{{ path('app_type_appart_delete', {'id': type_appart.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_appart.id) }}">
    <button class="btn btn-danger mt-2">Delete</button>
</form>
